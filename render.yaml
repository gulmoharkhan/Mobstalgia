services:
  - type: web
    name: mobstalgia
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: "node --experimental-sqlite server.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 22.11.0
